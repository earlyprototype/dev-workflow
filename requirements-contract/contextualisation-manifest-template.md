# Contextualisation manifest — <unit of work>

> The reading list to load *before* writing code, scoped to this unit of work.
> Read it, then write the summary at the bottom — the manifest is done when the
> summary exists, not when the list is assembled.

## Read in full
Everything you must actually read end-to-end before starting — the requirements
extract, the design for this screen, the interfaces you'll build against.

- <path / doc> — why it matters here

## Held as a constraint
Things you must not violate but don't need to re-read in full — the coding
standards, the design rulebook, the access rules, the source of truth.

- <path / doc> — the constraint it imposes

## Consulted only if touched
Read *only* if this unit of work reaches into them — adjacent modules, shared
utilities.

- <path / doc> — the trigger that would make you read it

## Existing implementations (required)
Any prior art that already solves part of this — a previous prototype, a legacy
system, a competitor, an open-source project. **Required reading**, not optional:
an untested predecessor is exactly where a silent regression or a lost edge case
hides. If there genuinely is none, say so explicitly.

- <what> — <what to learn or carry across from it>

## Summary (the deliverable)
Written after reading. A short, checkable statement of what was read and what
constraints follow from it — the evidence the observe step actually happened, not
a claim that it did.

>
